# Changesets

This directory stores release notes generated by [Changesets](https://github.com/changesets/changesets).

When you introduce a change that needs to be released, run `pnpm changeset` and follow the prompts. The CLI will add a Markdown file in this folder describing the update. Commit that file alongside your code changes.
